from setuptools import setup, find_packages

setup(
    name='abadder',
    version='0.1',
    packages=find_packages(),
    author='Your Name',
    author_email='your.email@example.com',
    description='A simple package with a sum function',
    long_description='A test package for PyPI publishing using GitHub Actions.',
    long_description_content_type='text/markdown',
    url='https://github.com/yourusername/abadder',  # Replace with your actual GitHub repo
    classifiers=[
        'Programming Language :: Python :: 3',
        'License :: OSI Approved :: MIT License',
        'Operating System :: OS Independent',
    ],
    python_requires='>=3.6',
)
